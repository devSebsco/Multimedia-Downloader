# Server
PORT=3001
NODE_ENV=development

# CORS — the frontend URL that will make requests
FRONTEND_URL=http://localhost:4321

# Temporary download directory
TEMP_DIR=./temp

# Path to yt-dlp binary (leave as 'yt-dlp' if in PATH)
YTDLP_PATH=yt-dlp

# Path to FFmpeg binary (leave empty to use @ffmpeg-installer/ffmpeg bundled)
FFMPEG_PATH=

# Log level: debug | info | warn | error (default: debug in dev, info in production)
LOG_LEVEL=debug

# YouTube: ruta local al archivo cookies.txt (formato Netscape, exportado del navegador)
# Opcional — si no se define, se usa solo el cliente Android
YOUTUBE_COOKIES_PATH=

# Instagram: URL completa del proxy residencial
# Formato: http://usuario:contraseña@host:puerto
# Requerido en producción para Instagram
INSTAGRAM_PROXY_URL=
